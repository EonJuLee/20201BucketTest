<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buckettest00">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SharemylistActivity"></activity>
        <activity android:name=".SharebucketActivity" />
        <activity android:name=".AddbucketActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".FindpwActivity" />
        <activity android:name=".InitialActivity" />
        <activity android:name=".SharelistPage" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".SharePage" />
        <activity android:name=".Mypage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>